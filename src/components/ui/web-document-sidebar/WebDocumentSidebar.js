import React from "react";

const WebDocumentSidebar = (props) => {

    return (
        <div style={{padding: '8px'}}>
            Search articles
        </div>
    );

}

export default WebDocumentSidebar;